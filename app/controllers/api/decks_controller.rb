class Api::DecksController < ApplicationController
  def index
    # if params[:category]
    #   @deck = Deck.where(category: params[:category]).first
    # else
    #   @deck = Deck.last
    # end
  end
end